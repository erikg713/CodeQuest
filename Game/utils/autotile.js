function autoTile(tileType, neighbors) {
    // Logic for autotiling based on neighboring tiles
}

function createAnimation(frames, frameRate, loop = true) {
    return { frames, frameRate, loop };
}

